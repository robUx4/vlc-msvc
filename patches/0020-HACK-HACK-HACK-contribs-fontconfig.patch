From 1df310b71bba3e048096e4441de4b3707cf35e27 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Hugo=20Beauz=C3=A9e-Luyssen?= <hugo@beauzee.fr>
Date: Wed, 22 Oct 2014 14:56:24 +0200
Subject: [PATCH 20/99] HACK HACK HACK: contribs: fontconfig

---
 contrib/src/fontconfig/rules.mak | 7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

diff --git a/contrib/src/fontconfig/rules.mak b/contrib/src/fontconfig/rules.mak
index 3c19996..78b92c7 100644
--- a/contrib/src/fontconfig/rules.mak
+++ b/contrib/src/fontconfig/rules.mak
@@ -50,8 +50,10 @@ DEPS_fontconfig = freetype2 $(DEPS_freetype2) libxml2 $(DEPS_libxml2)
 
 .fontconfig: fontconfig
 ifdef HAVE_WIN32
-	$(RECONF)
-endif
+	cd $< && $(MAKE) -C src install
+	sed -e 's%/usr/lib/libiconv.la%%' -i.orig $(PREFIX)/lib/libfontconfig.la
+	cp $</fontconfig.pc $(PREFIX)/lib/pkgconfig/
+else
 	cd $< && $(FONTCONFIG_ENV) ./configure $(FONTCONFIG_CONF)
 	cd $< && $(MAKE)
 ifndef HAVE_MACOSX
@@ -62,4 +64,5 @@ else
 	sed -e 's%/usr/lib/libiconv.la%%' -i.orig $(PREFIX)/lib/libfontconfig.la
 	cp $</fontconfig.pc $(PREFIX)/lib/pkgconfig/
 endif
+endif
 	touch $@
-- 
1.9.5.msysgit.1

